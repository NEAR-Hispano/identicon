const viewMethods = {
  getAssignedValidations: 'get_assigned_validations'
};

const changeMethods = {
  requestVerification: 'request_verification',
  cancelVerification: 'cancel_verification',
  registerAsValidator: 'register_as_validator',
  unregisterAsValidator: 'unregister_as_validator',
  assignValidators: 'assign_validators'
};

module.exports = {
  viewMethods,
  changeMethods
};